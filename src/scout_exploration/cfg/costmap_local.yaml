local_costmap:
  global_frame: robot/odom
  robot_base_frame: robot/base_link_fixed
  update_frequency: 0.5
  publish_frequency: 0.5
  static_map: false
  rolling_window: true
  width: 6.0
  height: 6.0
  resolution: 0.05

  obstacle_range: 2.5
  raytrace_range: 3.0
  inflation_radius: 0.55

  # observation_sources: base_scan
  # base_scan: {frame_name: robot/lidar, data_type: LaserScan, topic: /repub/scan, marking: true, clearing: true}